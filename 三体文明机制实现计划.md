# 三体文明机制实现计划

## 概述

基于刘慈欣小说《三体》的世界观，为现有的三体模拟器添加文明机制。文明将在行星上发展，受三恒星运动的影响，经历恒纪元/乱纪元的交替，面临毁灭与重生的循环。

## 核心机制设计

### 1. 文明实体 (Civilization)

**属性：**
- `name` - 文明名称（自动生成，如"三体文明第137号"）
- `population` - 人口数量（指数级增长）
- `techLevel` - 科技等级（0-100，影响生存能力）
- `resources` - 资源储备
- `stability` - 稳定性（0-100，影响文明存续）
- `age` - 文明年龄（模拟时间）
- `status` - 状态：发展中/脱水/毁灭/重生中
- `generation` - 文明代数（第几代文明）

**状态机：**
```
发展中 → (乱纪元/极端气候) → 脱水 → (恒纪元) → 浸泡恢复 → 发展中
       ↓
       (毁灭事件) → 毁灭 → 重生 → 新文明
```

### 2. 环境系统 (Environment)

**恒纪元/乱纪元判断：**
- 计算行星与三颗恒星的距离变化率
- 距离稳定 → 恒纪元（文明发展期）
- 距离剧烈变化 → 乱纪元（文明威胁期）
- 极端接近恒星 → 极热事件
- 极端远离恒星 → 极寒事件

**环境参数：**
- `temperature` - 行星温度（基于恒星距离计算）
- `chaosLevel` - 混乱程度（距离变化率）
- `era` - 当前纪元类型：恒纪元/乱纪元

### 3. 文明事件系统

**常规事件：**
- 人口增长（恒纪元）
- 科技进步（持续发展）
- 资源消耗（持续）
- 脱水保存（乱纪元触发）
- 浸泡恢复（恒纪元触发）

**随机事件：**
- 技术爆炸（科技等级大幅提升）
- 科技倒退（乱纪元影响）
- 文明内乱（稳定性下降）
- 宇宙广播（高等级文明可触发）
- 黑暗森林打击（广播后可能触发）

**毁灭事件：**
- 双星/三星凌日（行星被恒星吞噬）
- 极端气候（温度超出生存范围）
- 引力撕裂（过于接近大质量恒星）
- 黑暗森林打击（高级文明攻击）

### 4. 文明历史系统

**记录内容：**
- 每代文明的存续时间
- 毁灭原因
- 最高科技等级
- 最高人口数量
- 重大事件日志

**统计面板：**
- 文明总代数
- 平均存续时间
- 最长存续文明
- 技术爆炸次数
- 宇宙广播次数

## 实现步骤

### 第一步：数据结构定义

1. 创建 `Civilization` 类
   - 初始化属性
   - 状态转换方法
   - 资源/人口计算方法

2. 创建 `Environment` 类
   - 温度计算（基于恒星距离）
   - 纪元判断逻辑
   - 环境事件触发

3. 创建 `CivilizationHistory` 类
   - 事件记录
   - 统计数据
   - 历史查询

### 第二步：核心逻辑实现

1. **温度计算公式：**
   ```javascript
   temperature = Σ(starLuminosity[i] / distance[i]^2)
   ```

2. **纪元判断逻辑：**
   ```javascript
   chaosLevel = average(distanceChangeRate)
   era = chaosLevel < threshold ? "恒纪元" : "乱纪元"
   ```

3. **文明发展逻辑：**
   ```javascript
   if (恒纪元 && status === "发展中") {
     population *= growthRate
     techLevel += researchRate
     resources -= consumptionRate
   }
   ```

4. **脱水/浸泡逻辑：**
   ```javascript
   if (乱纪元 && chaosLevel > dangerThreshold) {
     status = "脱水"
     population *= survivalRate
   }
   if (恒纪元 && status === "脱水") {
     status = "发展中"
     population *= recoveryRate
   }
   ```

### 第三步：UI界面实现

1. **文明状态面板**（添加到控制面板）
   - 文明代数
   - 当前状态
   - 人口数量
   - 科技等级
   - 资源储备
   - 稳定性
   - 当前纪元
   - 行星温度

2. **事件日志面板**（右侧滚动显示）
   - 实时事件推送
   - 时间戳
   - 事件类型图标
   - 事件详情

3. **文明历史面板**（可展开）
   - 历代文明列表
   - 统计图表
   - 重大事件回顾

### 第四步：视觉效果

1. **行星状态指示**
   - 发展中：绿色光晕
   - 脱水：蓝色光晕
   - 毁灭：红色闪烁
   - 重生：金色光晕

2. **事件特效**
   - 技术爆炸：金色粒子爆发
   - 毁灭：爆炸效果
   - 宇宙广播：波纹扩散效果
   - 黑暗森林打击：光束效果

3. **纪元指示**
   - 恒纪元：暖色调背景
   - 乱纪元：冷色调背景

### 第五步：交互功能

1. **文明控制按钮**
   - 强制脱水（消耗资源）
   - 加速发展（消耗资源）
   - 发送广播（需高科技等级）

2. **参数调节**
   - 文明发展速度
   - 灾难发生概率
   - 脱水存活率

## 文件修改清单

### index.html 修改

1. **CSS样式添加**（约150行）
   - 文明面板样式
   - 事件日志样式
   - 历史记录样式
   - 状态指示样式

2. **HTML结构添加**（约100行）
   - 文明状态面板
   - 事件日志容器
   - 历史记录面板

3. **JavaScript类添加**（约800行）
   - `Civilization` 类
   - `Environment` 类
   - `CivilizationHistory` 类
   - `CivilizationManager` 类（整合管理）

4. **NBodySim3D类扩展**（约200行）
   - 添加文明管理器实例
   - 在step()中更新文明状态
   - 在draw()中渲染文明效果

5. **事件绑定**（约100行）
   - 文明控制按钮事件
   - 参数输入事件

## 关键算法

### 温度计算
```javascript
calcTemperature(planetPos, stars) {
  let temp = 0;
  const baseTemp = 100;
  for (const star of stars) {
    const dist = distance(planetPos, star.position);
    const luminosity = star.mass * 10;
    temp += luminosity / Math.max(dist * dist, 1);
  }
  return temp * baseTemp;
}
```

### 纪元判断
```javascript
calcEra(planetHistory, windowSize = 50) {
  if (planetHistory.length < windowSize) return "未知";
  let totalChange = 0;
  for (let i = 1; i < windowSize; i++) {
    const distChange = Math.abs(planetHistory[i] - planetHistory[i-1]);
    totalChange += distChange;
  }
  const avgChange = totalChange / windowSize;
  return avgChange < 0.5 ? "恒纪元" : "乱纪元";
}
```

### 文明存活判定
```javascript
checkSurvival(civilization, environment) {
  if (environment.temperature < -50 || environment.temperature > 150) {
    return { survive: false, reason: "极端气候" };
  }
  if (civilization.stability <= 0) {
    return { survive: false, reason: "社会崩溃" };
  }
  if (civilization.population <= 0) {
    return { survive: false, reason: "人口灭绝" };
  }
  return { survive: true };
}
```

## 配置参数

```javascript
const CIVILIZATION_CONFIG = {
  growthRate: 1.001,           // 人口增长率
  researchRate: 0.01,          // 科技研究速度
  consumptionRate: 0.1,        // 资源消耗率
  dehydrationSurvival: 0.1,    // 脱水存活率
  recoveryRate: 1.5,           // 浸泡恢复率
  techExplosionChance: 0.001,  // 技术爆炸概率
  broadcastTechReq: 80,        // 广播所需科技等级
  darkForestChance: 0.3,       // 广播后打击概率
};
```

## 测试要点

1. 文明在恒纪元正常发展
2. 乱纪元触发脱水机制
3. 极端气候导致文明毁灭
4. 文明毁灭后自动重生
5. 高科技文明可发送广播
6. 广播后可能触发黑暗森林打击
7. 历史记录正确保存
8. UI实时更新

## 预期效果

玩家将观察到：
- 文明在行星上诞生、发展
- 三恒星运动导致的恒纪元/乱纪元交替
- 文明在乱纪元中脱水求生
- 文明因极端气候或恒星吞噬而毁灭
- 新文明在废墟上重生
- 高级文明尝试宇宙广播
- 黑暗森林法则的残酷验证

这完美还原了《三体》小说中三体文明的生存困境。
